[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "pyTMD"
version = "3.0.9"
description = "Python-based tidal prediction software for estimating ocean, load, solid Earth and pole tides"
keywords = [
    "Ocean Tides",
    "Load Tides",
    "Pole Tides",
    "Solid Earth Tides",
    "Tidal Prediction",
]
authors = [
    { name = "Tyler Sutterley" },
    { email = "tsutterl@uw.edu" },
]
maintainers = [
    { name = "pyTMD contributors" },
]
license = "MIT"
license-files = ["LICENSE"]
readme = { file = "README.md", content-type = "text/markdown" }

requires-python = "~=3.9"
dependencies = [
    "h5netcdf",
    "lxml",
    "numpy",
    "pint",
    "platformdirs",
    "pyproj>=2.5.0",
    "scipy>=1.10.1",
    "timescale>=0.1.1",
    "xarray",
]

classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Physics",
    "Topic :: Scientific/Engineering :: Oceanography",
]

[project.urls]
Homepage = "https://pytmd.readthedocs.io"
Documentation = "https://pytmd.readthedocs.io"
Repository = "https://github.com/pyTMD/pyTMD"
Issues = "https://github.com/pyTMD/pyTMD/issues"

[project.optional-dependencies]
doc = [
    "docutils>=0.17",
    "graphviz",
    "ipympl",
    "myst-nb",
    "numpydoc",
    "sphinx-autobuild",
    "sphinx-argparse>=0.4",
    "sphinxcontrib-bibtex",
    "sphinx-design",
    "sphinx_rtd_theme",
]
all = [
    "cartopy",
    "ipyleaflet",
    "ipywidgets",
    "jplephem",
    "jupyterlab",
    "matplotlib",
    "notebook",
    "pandas",
]
aws = ["dask", "obstore", "pandas", "pyarrow", "s3fs", "zarr>=3"]
dev = [
    "dask",
    "flake8",
    "gh",
    "oct2py",
    "pytest>=4.6",
    "pytest-cov",
    "pytest-xdist",
]

[project.scripts]
"fetch_arcticdata.py" = "pyTMD.datasets.fetch_arcticdata:main"
"fetch_aviso_fes.py" = "pyTMD.datasets.fetch_aviso_fes:main"
"fetch_box_tpxo.py" = "pyTMD.datasets.fetch_box_tpxo:main"
"fetch_database.py" = "pyTMD.datasets.fetch_database:main"
"fetch_gsfc_got.py" = "pyTMD.datasets.fetch_gsfc_got:main"
"fetch_iers_opole.py" = "pyTMD.datasets.fetch_iers_opole:main"
"fetch_jpl_ssd.py" = "pyTMD.datasets.fetch_jpl_ssd:main"
"fetch_test_data.py" = "pyTMD.datasets.fetch_test_data:main"
"reduce_otis.py" = "pyTMD.datasets.reduce_otis:main"

[tool.setuptools.packages.find]
exclude = ["providers*", "run*", "test*"]

[tool.pytest.ini_options]
minversion = "6.0"
norecursedirs = ".git"
addopts = "-n auto --verbose --capture=no -W ignore::UserWarning"
python_files = [
    "test*.py",
]
testpaths = [
    "test",
]

[tool.coverage.run]
branch = true
source = [
    "pyTMD",
]
omit = [
    "doc/*",
    "providers/*.py",
    "pyTMD/datasets/*",
    "test/*",
]

[tool.coverage.report]
show_missing = true
precision = 2

[tool.ruff]
line-length = 80
indent-width = 4
exclude = [
    ".git",
    ".pixi",
    "build",
    "run",
    "test",
]

[tool.ruff.lint]
select = [
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # flake8-bugbear
    "B",
    # flake8-simplify
    "SIM",
    # isort
    "I",
    # ruff-specific
    "RUF",
]

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.format]
indent-style = "space"
line-ending = "auto"
docstring-code-format = false

[tool.pixi.workspace]
name = "pytmd"
authors = ["pyTMD contributors"]
channels = ["conda-forge"]
platforms = ["osx-arm64", "linux-64", "win-64"]

[tool.pixi.pypi-dependencies]
pyTMD = { path = ".", editable = true }

[tool.pixi.environments]
default = { solve-group = "default" }
all = { features = ["all"], solve-group = "default" }
aws = { features = ["all", "aws"], solve-group = "default" }
dev = { features = ["all", "dev"], solve-group = "default" }
doc = { features = ["all", "dev", "doc"], solve-group = "default" }

[tool.pixi.tasks.start]
cmd = "jupyter lab"
cwd = "doc/source/notebooks"
description = "Start JupyterLab in the notebooks directory"

[tool.pixi.tasks.export-env]
args = [{ "arg" = "flags", "default" = "" }]
cmd = "pixi workspace export conda-environment {{ flags }} > environment.yml"
description = "Export workspace to a conda environment file"

[tool.pixi.feature.doc.tasks.docs]
args = [
    { "arg" = "builder", "default" = "html" },
    { "arg" = "flags", "default" = "--jobs=auto" },
    { "arg" = "auto", "default" = "" },
]
cmd = "sphinx-{{ auto }}build --builder={{ builder }} {{ flags }} source build/{{ builder }}"
cwd = "doc"
description = "Create documentation with sphinx"

[tool.pixi.feature.doc.tasks.auto-docs]
depends-on = [
    { task = "docs", args = [
        "html",
        "--jobs=auto --open-browser --ignore='*.csv'",
        "auto",
    ] },
]
description = "Create documentation with sphinx and live-reload over a browser"

[tool.pixi.feature.doc.tasks.clean-docs]
depends-on = [
    { task = "docs", args = ["html", "--jobs=auto --fresh-env --write-all", ""] },
]
description = "Remove build artifacts and rebuild the documentation with sphinx"

[tool.pixi.feature.doc.tasks.linkcheck-docs]
depends-on = [
    { task = "docs", args = ["linkcheck", "", ""] },
]
description = "Check documentation for broken links using sphinx"

[tool.pixi.feature.dev.tasks.fetch]
args = [{ "arg" = "timeout", "default" = "360" }]
cmd = "python -c 'import pyTMD; pyTMD.datasets.fetch.test_data(timeout={{ timeout }})'"
default-environment = "dev"
description = "Fetch data needed to run the pytest suite"

[tool.pixi.feature.dev.tasks.test]
args = [
    { "arg" = "flags", "default" = "" },
    { "arg" = "test", "default" = "." },
]
cmd = "pytest {{ flags }} {{ test }}"
cwd = "test"
default-environment = "dev"
description = "Run the pytest suite"

[tool.pixi.feature.dev.tasks.coverage]
cmd = "pytest -n=0 --cov=pyTMD --junitxml=pytest.xml --cov-report=xml test"
default-environment = "dev"
description = "Run the pytest suite and create coverage reports"

[tool.pixi.feature.dev.tasks.lint]
args = [
    { "arg" = "flags", "default" = "--select=E9,F63,F7,F82 --show-source" },
    { "arg" = "test", "default" = "." },
]
cmd = "flake8 {{ test }} --count {{ flags }} --statistics --extend-exclude 'build/,run/,*.ipynb_checkpoints/,.pixi/'"
default-environment = "dev"
description = "Run flake8 to check for compilation errors"

[tool.pixi.dependencies]
h5netcdf = "*"
lxml = "*"
numpy = "*"
pint = "*"
platformdirs = "*"
pyproj = ">=2.5.0"
scipy = ">=1.16.0"
timescale = ">=0.1.1"
xarray = "*"

[tool.pixi.feature.all.dependencies]
cartopy = "*"
ipyleaflet = "*"
ipywidgets = "*"
jplephem = "*"
jupyterlab = "*"
matplotlib-base = "*"
notebook = "*"
pandas = "*"

[tool.pixi.feature.aws.dependencies]
dask = "*"
obstore = "*"
pandas = "*"
pyarrow = "*"
s3fs = "*"
zarr = ">=3"

[tool.pixi.feature.dev.dependencies]
dask = "*"
flake8 = "*"
gh = "*"
oct2py = "*"
pytest = "*"
pytest-cov = "*"
pytest-xdist = "*"

[tool.pixi.feature.doc.dependencies]
docutils = ">=0.17"
graphviz = "*"
ipympl = "*"
myst-nb = "*"
numpydoc = "*"
sphinx-autobuild = "*"
sphinx-argparse = "*"
sphinxcontrib-bibtex = "*"
sphinx-design = "*"
sphinx_rtd_theme = "*"
